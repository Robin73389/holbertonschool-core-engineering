#!/usr/bin/env python3

def print_list_integer(my_list=[]):
    """_summary_

    Args:
        my_list (list, optional): _description_. Defaults to [].
    """
    for i in my_list:
        print("{:d}".format(i))
